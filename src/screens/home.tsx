import { Box } from 'native-base';

const Home = () => {
  return (
    <Box
      style={{
        flex: 1,
        alignContent: 'center',
        justifyContent: 'center',
        marginHorizontal: '7%',
        marginVertical: '10%',
      }}
    >
      Hello world!
    </Box>
  );
};

export default Home;
