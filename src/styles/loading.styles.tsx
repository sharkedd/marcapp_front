import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      paddingVertical: '5%',
      textAlign: 'center',
      fontSize: 24,
      fontWeight: '700',
    },
});

export default styles;